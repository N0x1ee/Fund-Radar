# ---------------------------------------------------------------------------
# CORE — needed to load data and run the API (the demo). Loose versions so pip
# downloads ready-made wheels for your Python (no compiler required).
# ---------------------------------------------------------------------------
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.9
pydantic-settings>=2.5
SQLAlchemy>=2.0.36          # 2.0.36+ ships wheels for current Python versions
openpyxl>=3.1

# ---------------------------------------------------------------------------
# AUTH — user accounts (feature/authentication). Required once auth is enabled.
# ---------------------------------------------------------------------------
bcrypt>=4.0
PyJWT>=2.8
email-validator>=2.0

# ---------------------------------------------------------------------------
# SCRAPER + AI — NOT installed in the website image (the site only serves the
# dashboard; it never scrapes or calls the AI). These are installed only by the
# every-2-days GitHub Action (.github/workflows/scrape.yml), which is where the
# scraping and AI extraction actually run. Keeping them out of the web image
# makes Render / Hugging Face builds smaller, faster and more reliable.
#
#   httpx>=0.27
#   beautifulsoup4>=4.12
#   pypdf>=4.0                # read text from PDF-based funding schemes
#   google-generativeai>=0.8  # Gemini SDK (Groq uses stdlib urllib, no package)
#   playwright>=1.47          # only if scraping JS-heavy sites (needs `playwright install`)
